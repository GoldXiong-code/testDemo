# Atoms-Demo — 产品文档

> **最后更新**：2026年8月23日

---

## 一、项目简介

**Atoms-Demo** 是一款 AI 驱动的产品构建平台，目标是帮助用户在几分钟内将创意转化为可销售的产品方案。用户通过自然语言对话即可完成产品规划、方案生成和图像设计，无需编码经验。

---

## 二、核心功能

### 2.1 用户系统
- **注册功能**：用户可以使用邮箱注册账号
- **登录功能**：已注册用户登录系统
- **用户管理**：基本的用户信息管理

### 2.2 AI/LLM 连接
- **大模型连接**：支持连接各种大型语言模型（LLM）
- **模型配置**：用户可以配置和管理不同的 LLM 连接
- **API 密钥管理**：安全存储和管理用户的 API 密钥

### 2.3 内容生成
- **输入框**：用户可以输入想法、需求或描述
- **AI 生成**：根据用户输入，调用 LLM 生成结果
- **结果展示**：清晰展示 AI 生成的内容

### 2.4 跨平台支持
- **Windows**：支持所有内核的 Windows 电脑
- **macOS**：支持 Mac 电脑
- **Web 浏览器**：通过浏览器访问，无需安装额外软件

---

## 三、分类体系

### 3.1 AI 意图分类

| 一级分类 | 图标 | 二级说明 |
|---------|------|---------|
| 方案生成 | 📝 | 产品方案、市场分析、功能规划、定价策略 |
| 图像生成 | 🎨 | 根据描述生成真实图片（通义万相） |
| 问答对话 | 💬 | 直接回答用户的各类问题 |

---

## 四、技术栈

> **选定方案**：Next.js + Vercel（方案A）

| 层级 | 技术 | 用途 |
|------|------|------|
| 前端框架 | Next.js 16 (App Router) | 全栈 React 框架，内置路由和 API |
| 编程语言 | TypeScript | 类型安全的 JavaScript |
| UI 框架 | React 19 | 构建用户界面 |
| 样式方案 | Tailwind CSS 4 | 实用优先的 CSS 框架 |
| 数据库 | SQLite + Prisma | 存储用户数据和模型定义 |
| ORM | Prisma 7 | 数据库访问层 |
| 图标库 | Lucide React | 提供界面图标 |
| 表单校验 | Zod | 表单数据校验 |
| 密码加密 | bcrypt | 密码安全加密 |
| AI 服务 | 阿里云通义千问 (DashScope) | 文本生成（qwen3.7-flash）+ 图片生成（wanx2.1-t2i-turbo） |
| AI Agent | 多 Agent 协作（意图识别→分发） | 自动判断用户意图并路由到专业 Agent |
| 部署 | PM2 + Nginx | 进程管理 + 反向代理，运行在阿里云轻量服务器 |

---

## 五、项目结构

```
atoms-demo/
── src/
│   ├── app/                          # 页面路由 (Next.js App Router)
│   │   ├── page.tsx                  # 首页（Hero + Navbar）
│   │   ├── layout.tsx                # 全局布局
│   │   ├── globals.css               # 全局样式 + CSS 变量
│   │   ├── login/
│   │   │   └── page.tsx              # 登录页
│   │   ├── register/
│   │   │   └── page.tsx              # 注册页
│   │   ├── app/
│   │   │   └── page.tsx              # AI 生成工作台
│   │   └── api/                      # API 路由
│   │       ├── auth/
│   │       │   ├── register/route.ts # 注册接口
│   │       │   ├── login/route.ts    # 登录接口
│   │       │   └── check-username/
│   │       │       └── route.ts      # 用户名检查接口
│   │       └── agent/route.ts        # AI Agent 接口（多 Agent 协作）
│   ├── components/                   # 可复用组件
│   │   ├── Navbar.tsx                # 顶部导航栏
│   │   └── Hero.tsx                  # 首页主视觉区域
│   └── lib/                          # 工具库
│       ├── db.ts                     # Prisma 数据库客户端
│       ├── store.ts                  # 客户端状态管理 (localStorage)
│       └── validators.ts            # Zod 表单校验规则
── prisma/
│   └── schema.prisma                 # 数据库模型定义
├── prisma.config.ts                  # Prisma 配置
├── data/
│   └── db.json                       # SQLite 数据文件
└── package.json                      # 项目配置
```

---

## 六、⚡ 重要规则：技术决策流程

**本项目最重要的规则**——用户在技术方面的决策方式：

1. **任何涉及技术选择的决策**（选什么库、用什么方案、架构怎么设计等），开发人员（Claude）必须：
   - 列出至少 2-3 个可行方案
   - 用通俗易懂的语言解释每个方案的原理、优点、缺点
   - 给出自己的推荐并说明理由
   - **由用户来做最终决定**

2. **禁止的行为**：
   - 不要假设用户知道技术术语，必须解释
   - 不要在用户没有确认的情况下直接采用某个技术方案
   - 不要替用户做技术决策
   - 不要擅自修改与用户当前要求无关的内容

3. **沟通原则**：
   - 用生活化的类比来解释技术概念
   - 每次只让用户做一个关键决策，不要一次抛太多问题
   - 给出明确推荐，标注"(推荐)"，让用户知道什么是常见做法

---

## 七、开发路线图

- [x] 阶段 0：产品文档设计与技术选型
- [x] 阶段 1：项目初始化（Next.js + TypeScript + Tailwind CSS）
- [x] 阶段 2：UI 开发（首页、导航栏、登录注册页）
- [x] 阶段 3：用户系统（注册、登录、用户名唯一性检查、bcrypt 加密）
- [x] 阶段 4：AI 功能（意图识别 Agent、方案生成、SVG 图像生成）
- [x] 阶段 5：图像生成改进（SVG 代码 → 通义万相真实 PNG 图片）
- [x] 阶段 6：首页白屏修复（Google Fonts → 系统字体）
- [x] 阶段 7：部署到阿里云轻量应用服务器（PM2 + Nginx）
- [x] 阶段 8：导航栏精简（移除定价/关于/社区/资源链接）
- [ ] 阶段 9：HTTPS 配置（Let's Encrypt）
- [ ] 阶段 10：API 密钥安全改造（硬编码 → 环境变量）

---

## 八、真实交互与数据持久化

### 8.1 基本使用流程

| 步骤 | 用户操作 | 系统响应 | 数据持久化 |
|------|---------|---------|-----------|
| 1 | 访问首页 | 展示 Hero 区域 + 输入框 | 无 |
| 2 | 点击注册 | 填写用户名/邮箱/密码 | 写入 SQLite（bcrypt 加密密码） |
| 3 | 登录系统 | 验证凭证，保存会话到 localStorage | 读取 SQLite 用户表 |
| 4 | 输入想法，点击生成 | AI Agent 识别意图（方案/画图/问答） | 无（当前不保存对话历史） |
| 5 | 查看生成结果 | SSE 流式输出，Markdown 渲染或图片展示 | 无 |
| 6 | 复制/下载/重新生成 | 本地操作 | 无 |

**数据持久化现状**：
- ✅ 用户数据（账号、密码）持久化在 SQLite
- ❌ 对话记录、生成内容不持久化（刷新页面即丢失）
- ⚠️ 当前会话状态保存在浏览器 localStorage

### 8.2 延展能力：AI 画图

用户输入"画一个xxx"后，系统自动识别画图意图，调用通义万相 API 生成真实 PNG 图片：

| 环节 | 技术实现 |
|------|---------|
| 意图识别 | 意图 Agent 调用通义千问，返回 `intent: "image"` |
| 图片生成 | 提交异步任务 → 轮询状态 → 获取图片 URL |
| 结果展示 | `<img>` 标签渲染，支持复制和下载 |
| 模型 | wanx2.1-t2i-turbo（通义万相，1024×1024） |

### 8.3 如果继续投入，如何扩展？

参考 Atoms.dev 的完整产品生命周期（研究 → 构建 → 部署 → 营销 → 变现），按优先级排序：

#### 🔴 第一优先级：基础体验完善（1-2 周）

| 功能 | 工作量 | 价值 |
|------|--------|------|
| 生成历史记录保存 | 2-3 天 | 用户能回看之前的生成内容 |
| HTTPS 配置 | 1 天 | 安全 + 浏览器信任 |
| API 密钥环境变量化 | 0.5 天 | 安全改进 |
| 域名备案 + DNS 配置 | 等管局审核 | 正式可用 |
| 方案导出 PDF | 2-3 天 | 用户可以带走成果 |

#### 🟡 第二优先级：商业化能力（2-3 周）

| 功能 | 工作量 | 价值 |
|------|--------|------|
| 支付集成（Stripe/支付宝） | 3-5 天 | 用户付费使用 |
| 订阅制计费 | 3-5 天 | 持续收入 |
| 多模型支持（GPT-4、Claude） | 2-3 天 | 更灵活的 AI 选择 |
| 用户 API Key 配置 | 1-2 天 | 降低服务器成本 |
| 用量统计仪表盘 | 2-3 天 | 用户查看使用情况 |

#### 🟢 第三优先级：增长与营销（3-4 周）

| 功能 | 工作量 | 价值 |
|------|--------|------|
| 产品落地页生成 | 3-5 天 | 用户生成方案后一键生成落地页 |
| SEO 自动优化 | 2-3 天 | 搜索引擎排名 |
| 分享链接 + 社交传播 | 1-2 天 | 用户分享方案，带来新用户 |
| 模板市场 | 5-7 天 | 预制方案模板，降低使用门槛 |
| 产品截图/视频生成 | 3-5 天 | 帮用户做宣传素材 |
